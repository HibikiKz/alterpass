import 'package:flutter/material.dart';

class SettingsActivity extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      key: Key("Settings Activity"),
      appBar: AppBar(
        title: Text("Settings")
      ),
      body: Column(
        children: <Widget>[
        ]
      )
    );
  }
}
